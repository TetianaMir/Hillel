Это оператор повторения, делает многочисленные копии списка и все их объеденяет. Формат операции:
список * n(кол-во копий)

list - [1] * 3
print(list)
[1,1,1]

Также его еще называют оператором распаковки итерируемых объектов
list = [1,2,3,4,5}
print(*list)
1 2 3 4 5

А также используется:
 * для передачи аргументов в функцию;
* для сбора переданных в функцию аргументов;
* при распаковке кортежей;
